require "bundler/gem_tasks"
task :default => :spec

#Añadido por mí

desc "Hacer pruebas TDD sobre etiqueta_nutricional.rb con rspec"
task :TDD do
	sh "rspec -I. spec/etiqueta_nutricional_spec.rb"
end 
